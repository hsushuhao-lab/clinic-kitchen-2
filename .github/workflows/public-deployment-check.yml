name: Public game verification
on:
  workflow_run:
    workflows: ['Runtime-only web release audit']
    types: [completed]
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  live-browser:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Install Chromium
        run: |
          python -m pip install -r requirements-qa.txt
          python -m playwright install --with-deps chromium
      - name: Verify live source SHA and every runtime file
        run: python tools/verify_public_build.py --url https://hsushuhao-lab.github.io/clinic-kitchen-2/ --expect-sha $(git rev-parse HEAD)
      - name: Play published serial clinic and night shift using real input
        run: python tests/test_clinic_flow.py --base-url https://hsushuhao-lab.github.io/clinic-kitchen-2/ --output qa/public-clinic
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: public-clinic-game-evidence
          path: qa/public-clinic/
          retention-days: 14
